// declaration

/**
 * input
 * ```
 *
 * master: {
 *  property_name: {
 *    name: 'property-name',
 *    value: 'default value'
 *  }
 * }
 *
 * instances: [{
 *  property_name: {
 *   name: "property-name"
 *   value: 'override value'
 *  }
 * }]
 *
 * ```
 *
 * returns
 *
 *
 */
export function mark_as_managed_property_if_one_is_override() {
  //
}

// scoped_design_input_file
interface ScopedDesignInputFile {
  readonly file: string;
  components: {};
  nodes: {};
}

// 1.
// diff first, extract later

// 2.
// extract with diff
