import csstree from "css-tree";

/**
 * selects the text elements that is targeted to be applied for textfit
 * @param css the css file as a string
 * @param selectors the element query selectors
 * @param options
 */
export function select(
  css: string,
  selectors: string[],
  options: {
    mode: "fixed";
    widthOnly: boolean;
  }
) {
  //
}
