# Plugin Core - `@code-plugin/core`

Official code plugin core for extending Grida Code's functionality

## Usage

```ts
import Plugin from "@code-plugin/core";

export default class MyPlugin extends Plugin {
  apply(context) {
    // ...
  }
}
```
